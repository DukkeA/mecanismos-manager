import {requireMember} from '@/server/auth';import {recordsPage} from '@/server/records-query';import {AccessDenied} from '@/domain/permissions';import {ZodError} from 'zod';
export async function GET(request:Request){const headers={'Cache-Control':'private, no-store'},actor=await requireMember().catch(()=>null);if(!actor)return Response.json({error:'Sesión requerida.'},{status:401,headers});try{return Response.json(await recordsPage(actor,new URL(request.url).searchParams),{headers});}catch(e){return Response.json({error:e instanceof AccessDenied?e.message:e instanceof ZodError?'Revisa los filtros.':'No se pudo consultar la tabla.'},{status:e instanceof AccessDenied?403:400,headers});}}
